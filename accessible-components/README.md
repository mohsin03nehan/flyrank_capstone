# Accessible Components Playground

A small React + TypeScript playground that demonstrates accessible UI patterns such as buttons, accordions, dialogs, and form controls. The goal is to keep semantics, keyboard support, and visible focus states front and center.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run lint`
